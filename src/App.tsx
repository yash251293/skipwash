import { useRef } from "react";
// import "./App.css"; // CSS import commented out
// import Navbar from "./components/Navbar/Navbar"; // Navbar import commented as it's not used
// import { Outlet } from "react-router-dom"; // Outlet import commented as it's not used
// import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

// const queryClient = new QueryClient();

function App() {
  return (
    <div style={{color: 'black', fontSize: '24px', textAlign: 'center', paddingTop: '40px', backgroundColor: 'lightgoldenrodyellow', height: '100vh'}}>
      App.tsx is rendering this (CSS unlinked)!
    </div>
  );
}

export default App;
